# FareAlert 使用指南（组件介绍 · 搭建教程 · 日常使用）

> 本指南面向第一次接触本组件的用户：从零搭建 → 配置推送 → 自动监控 → 手机查看，一条龙讲完。
> 对应版本 v0.27（覆盖率合并口径 + /api/health 部署可观测 + 顶栏数据新鲜度指示）。

## 一、FareAlert 是什么

一句话：**帮你盯低价的私人小雷达**。

- 定时查询未来 N 天（默认 60 天）**机票每日最低价**，口径为最终支付价（裸价 + 机建 + 燃油，当前税费合计 120 元）
- 低于你设的心理价位 → 立即推送到 iPhone（Bark）/ 微信（ServerChan），通知直达购票页
- 同时查询 12306 列车**全席位票价**（二等座/卧铺/硬座等,接口查到什么就列什么），自动算**学生票估算价**，和机票摆在一起告诉你"出行最优"选哪个
- 按航司标注**是否含免费托运**，廉航/特价舱自动加 ⚠️ 提醒

### 工作流程

```
去哪儿价格日历 ┐
              ├→ Python 后端：算总价 → 对比阈值/动车 → 低于价位 → Bark/微信推送
12306 票价查询(全席位) ┘    ↓
                    Web 仪表盘（热力日历 / 趋势图 / 最优对比 / 可视化配置）
```

### 组件构成

| 组件 | 位置 | 作用 |
| --- | --- | --- |
| 后端流水线 | `main.py` | 一次查询 = 拉价格 → 算含税总价 → 判阈值 → 推送 |
| Web 面板 | `webui.py` + `webui/` | 浏览器里的仪表盘与设置页（无需构建工具） |
| 核心模块 | `core/` | 数据源抓取、判定去重规则、推送通道、报告生成 |
| 部署脚本 | `deploy/`、`start_windows.bat`、`start_mac.sh` | 计划任务注册/卸载、一键启动 |
| 个人配置与数据 | `config.json`、`data/` | 你的线路/价位/推送 key 与运行数据（已 gitignore，不会上传） |

环境要求：Python 3.10+ 和两个第三方库（requests、flask）。无数据库、无 GPU、无前端构建，普通电脑、旧笔记本、NAS、树莓派都能跑。

## 二、Windows 搭建（推荐作为主力监控机）

### 步骤 1：安装 Python

到 [python.org](https://www.python.org/downloads/) 下载 3.10 以上版本，安装时**务必勾选 "Add python.exe to PATH"**。验证：

```powershell
python --version
```

### 步骤 2：安装依赖

```powershell
cd C:\Users\你的用户名\fare-alert
pip install -r requirements.txt
```

### 步骤 3：首次试跑（只查询、不推送）

```powershell
python -X utf8 main.py --query
```

首次运行会自动生成 `config.json`（个人配置模板），并打印未来 60 天最低价表。能看到价格说明数据源连通。

### 步骤 4：启动 Web 面板

双击项目目录里的 **`start_windows.bat`**（等价于 `python -X utf8 webui.py`），浏览器自动打开 http://127.0.0.1:8765 。

### 步骤 5：设置线路与心理价位

进入面板"🛫 线路管理"页：

- 出发/到达城市：必须填**中文城市名**（如 杭州 / 重庆），机场代码查不到价
- 查询窗口：默认 60 天，可 1–365
- 心理价位：按**含税总价**填（当前最低机票总价 ¥420，价位设 350 则不提醒，设 450 才会触发）
- 列车对比车站对：点"+ 添加车站对"新增一行，出发站/到达站**支持中文/拼音联想**(如输入 hzd 选"杭州东")，从下拉中选择即精确匹配；城市输入同样支持联想；出发⇄到达可一键互换

改完点"保存线路"。

### 步骤 6：配置 iPhone 推送

1. App Store 搜索 **Bark** 安装，打开 App 复制里面的 Key（一串字符）
2. 面板"🔔 提醒推送"页 → Bark Key 粘贴 → 保存推送设置
3. 点"发送测试推送"，手机立刻响 = 通道打通

（可选）ServerChan 微信推送：sct.ftqq.com 免费申领 SendKey，同样填入即可，两个通道可同时开。

### 步骤 7：挂上自动监控（后台定时查询）

以管理员身份打开 PowerShell：

```powershell
cd C:\Users\你的用户名\fare-alert
powershell -ExecutionPolicy Bypass -File deploy\register_task.ps1
```

之后 Windows 计划任务每 45 分钟静默查询一次（无窗口、无打扰），命中价位即推送。查询间隔在"🔌 数据源"页修改，改完重新执行一次注册脚本即可。卸载监控：执行 `deploy\remove_task.ps1`。

### 步骤 8（可选）：手机浏览器看面板

1. 面板"🔌 数据源"页把 Web UI Host 改为 `0.0.0.0` 并保存
2. Windows 防火墙放行 Python（专用网络）
3. 手机 Safari 访问 `http://电脑局域网IP:8765`（电脑 IP 用 `ipconfig` 查）
4. Safari 分享 → **添加到主屏幕**，即得到一个 App 图标，点开就是全功能面板
5. （v0.20 起）主屏图标为独立 PNG 纸飞机图标；加载过一次后，即使电脑短暂离线或重启中，手机也能离线阅读最近一屏的日历与周报数据（Service Worker 缓存兜底，数据不会凭空变新）

## 三、Mac 搭建

把整个项目文件夹拷到 Mac（U 盘/网盘/git clone 均可），然后：

```bash
cd fare-alert
pip3 install -r requirements.txt
bash start_mac.sh        # 启动 Web 面板
```

后台常驻轮询（不开面板）：`python3 main.py --loop`；或用 crontab 定时：`*/45 * * * * cd /path/to/fare-alert && python3 main.py --once`。

## 四、iPhone 上的存在形态

iPhone **不运行任何后端**，它只做两件事，因此省电省心：

- **推送通知（主角）**：低于价位秒收，通知里带日期/航班/含税总价/托运提示，点开直达去哪儿购票页
- **主屏幕面板**：局域网内随时打开仪表盘看 60 天日历与趋势

## 五、日常使用

### 面板五个页签

| 页签 | 用途 |
| --- | --- |
| 📊 仪表盘 | 城市图鉴 Hero 卡（出发/到达城市介绍）/ KPI 卡片（含最低卧铺）/ 60 天热力日历与低价柱状图一键切换（点任意日期看航班+托运+购票链接+列车参考）/ 价格趋势图（红虚线=心理价位）/ 列车全席位对比表 / "出行最优"判定 |
| 🛫 线路管理 | 增删改监控线路，支持同时盯多条；快捷模板一键添加热门线路；城市/车站拼音联想、一键互换 |
| 🔌 数据源 | 数据源开关、机建燃油税费、查询频率、Web 监听地址 |
| 🔔 提醒推送 | Bark/ServerChan 配置、测试推送、最近 50 条提醒历史 |
| 📜 日志 | 运行日志尾部，排障用 |

页头"立即查询"按钮：手动触发一次完整查询（约 5–20 秒），刷出新快照。

### 提醒规则（防轰炸）

- 某日期**首次**低于价位才推；同一日期 **6 小时冷却**内不重复推
- 已提醒过的日期若**再降 5 元以上**，会再推一次
- 每轮最多推**最低的前 5 个**日期
- 以上参数在 `config.json` 的 `alert` 段可调（top_n / realert_drop / cooldown_hours）

### 命令行速查

```powershell
python -X utf8 main.py --once       # 查一轮并按规则推送
python -X utf8 main.py --query      # 只看价格表，不推送
python -X utf8 main.py --loop       # 常驻轮询（间隔+抖动见配置）
python -X utf8 main.py --test-push  # 发送测试推送
```

### 一键直达购票（v0.4 新增）

仪表盘里的所有选项都可以点击直达购票页：

- 顶部 KPI 卡（最低机票/二等座/学生价/最低卧铺）：整卡可点，机票跳去哪儿下单页，车次跳 12306 预填查询页
- “出行最优”对比卡：卡片底部“去查票/下单 →”链接
- 列车对比表：每行末尾“下单”按钮，自动带入日期与车站
- 日历日详情：“去哪儿购票页 →”按钮

机票的起降时刻与时长（v0.19 起四级链路）：

1. **机场班期板兜底（零密钥，自动开启）**：杭州出发/到达的线路自动读取萧山机场官网公开班期板（与浏览器打开官网同源、只读、6 小时缓存、每天最多 2 次请求），按「航班号 + 星期几」沉淀计划时刻库。详情卡有时刻时打灰底「计划时刻·机场班期」徽标；这类时刻是航季排班参考（计划值，非实时验证）。
2. **跨日班期借用（v0.19，自动）**：目标日期对应的星期还没沉淀时，自动借用同一航班号其他星期的班期时刻——同航季内同号航班时刻通常一致，详情卡打琥珀色「跨日班期·参考」徽标（悬停有说明）。首周内也能看到大部分航班时刻，跑满一周后逐步被精确星期数据替换。
3. **Amadeus 时刻表（可选升级）**：配置免费 Key 后自动优先使用实时刻（蓝底「计划时刻·Amadeus」徽标），覆盖更全、含中转航段匹配。
4. **距离估算兜底**：以上都没有时按大圆距离估算飞行时长（"约2h30m(估)"）；已有起飞时刻但缺落地时，落地显示 ~ 前缀的估算值（虚线下划线，悬停说明"以购票页为准"），两者都缺时起降显示"--:--"与「时刻待接入」。

车次的出发/到达时刻与历时来自 12306，表格中直接展示。

### 国际航线与 Amadeus 配置（v0.7 新增）

- **免 Key 即可用**：线路勾选"国际航线"后，自动接入去哪儿国际特价日历——每月约 3–5 个真实含税促销价，无需任何注册
- **想要全日期覆盖**：到"数据源"页按 3 步向导配置免费 Amadeus Key（developers.amadeus.com 注册 → My Apps 创建 → 填入 Key/Secret 点"测试连接"，注意官方域名是复数 developers，单数域名已停用会显示 application is not available），国际线升级为全日期含税价 + 真实起降时刻
- **国内线也受益**：配 Key 后国内线路的缺价日期自动用 Amadeus 补洞（24h 缓存保护免费额度）

### 缺价日期的参考价补全 + 移动端提醒（v0.8 新增）

- **为什么有的日期标着"临近日参考"**：数据源日历对部分日期没有缓存价（显示"查价"），按合规原则不模拟请求；这些日期自动镜像最近有价日的价格作展示参考，并标注距离天数（如"距 12 天"）。参考价**只用于补全日历显示**，不触发推送提醒、不参与"出行最优"判定；点击可直达当日购票页查实价
- **柱状图视图**：日历卡右上角可在"日历/柱状"间切换——柱越高越便宜，绿色=低于心理价位，斜纹=参考价
- **移动端低价提醒**：手机浏览器打开面板时，出现新的低价日期会弹浮动横幅（可点"查看"直达），支持手机震动；点页头"通知"按钮授权后，即使面板在后台也能收到系统级通知
- **城市图鉴**：仪表盘顶部展示出发/到达城市的英文名、一句话介绍与旅行海报剪影（湖山/天际线/古建/海滩/群山/双塔 6 种场景，离线渲染）；若你的网络能访问 Wikipedia，城市实拍图会自动叠加在剪影上
- **航班真实起降时刻**：需要 Amadeus 免费 Key（注意官方域名是 **developers**.amadeus.com，单数域名已停用）；配好后详情卡显示 24 小时制的起飞/落地时间，中转行程按航段匹配；未配 Key 时时间轴显示"时刻待接入"徽标与飞行时长估算

### API 注册地址一览 + 趋势图新版（v0.9 新增）

**到底哪些要 Key、去哪注册：**

| 数据/能力 | 是否需要 Key | 注册/获取地址 | 说明 |
|---|---|---|---|
| 国内机票日历（去哪儿网关） | 免 Key | 无需注册 | 开箱即用 |
| 12306 车次与席位 | 免 Key | 无需注册 | 开箱即用 |
| 国际航线全日历 + 真实起降时刻（Amadeus） | 需免费注册 | 注册：<https://developers.amadeus.com/register> · 我的应用：<https://developers.amadeus.com/my-apps> | 邮箱验证即可，无需信用卡；建 App 后复制 Key/Secret 填入"数据源"页 |
| 目的地天气（Open-Meteo） | 免 Key | 无需注册 | v0.10 起仪表盘自动显示目的地未来 7 天天气 |
| 汇率参考（open.er-api） | 免 Key | 无需注册 | v0.10 起国际线自动显示 ¥1000≈当地货币 |
| iPhone 推送（Bark） | 免注册 | App Store 搜 Bark 或 <https://apps.apple.com/app/bark/id1403753865> | App 内复制 Key 填入"提醒推送"页 |
| 微信推送（ServerChan） | 需扫码登录 | <https://sct.ftqq.com/> | 复制 sct 开头 SendKey 填入"提醒推送"页 |
| Skyscanner 官方 API | 个人不可自助接入 | <https://partners.skyscanner.net> | 面向企业合作伙伴审批制；本项目用免 Key 数据源实现同类体验 |

**v0.10 界面更新**：趋势图改为 Google Flights 式扁平风——纯色靛蓝细线、去渐变去辉光去动画，均价线为极简点状灰线，最低价直接用深绿文字标注；柱状图同步改纯色。新增两个组件：

- **低价日 TOP5 表**：按含税总价排序的前 5 天，含日期/周几/总价/航司航班号/托运提示/直达购票按钮，低于心理价位的行整行淡绿高亮
- **目的地情报卡**：未来 7 天天气（emoji + 高低温 + 降水概率，Open-Meteo 免 Key）；国际线额外显示参考汇率（¥1000≈当地货币，open.er-api 免 Key）；均有本地缓存（天气 3h / 汇率 12h）

**Amadeus 隐私说明**：Amadeus 注册要求工作邮箱并校验所属公司域名；若介意可完全跳过——国内监控、天气、汇率、推送全部免注册可用，Amadeus 仅影响国际线的全日历覆盖与真实起降时刻。

**v0.11 无价日期补全（插值估算价）**：部分日期源端价格日历本身没有缓存价（灰色"待"），此前只能显示临近日参考价。v0.11 起，若缺价日期**两侧 14 天内都有真实价**，会按距离线性插值算出估算价（取整到 10 元），并以琥珀色斜纹 + "两侧真实价插值"徽标醒目标注；仍无两侧价的沿用灰色"临近日参考"。插值价与参考价一样**只作展示**：不触发低价提醒、不参与最低价/均价/TOP5 统计，点进去仍直达购票页查当日实价。趋势图上插值点为琥珀色空心圆点，鼠标悬停标注"插值估算"。

## 六、常见问题

- **推送收不到**：先点"发送测试推送"；检查 Bark Key 是否完整、系统通知权限是否开启；计划任务是否在跑（看"日志"页）
- **价格和 OTA 券后价对不上**：日历接口是挂牌价，不含平台券，通常差小几十；收到提醒后进购票页看券后实付
- **为什么按含税总价判定**：你最终付的就是裸价+机建+燃油，按支付口径比心理价位才有意义；税费变了在"数据源"页改
- **学生票准吗**：12306 查询接口不分学生价，此处按 动车二等座75折/硬座5折/硬卧=卧铺-硬座半价 估算（略偏低），资格与优惠区间以 12306 下单页为准
- **托运怎么看**：推送和日历详情里按航司标注；带 ⚠️ 的（春秋/九元/西部特价等）下单前务必确认
- **国际线为什么日期稀疏**：免 Key 特价源只返回有促销的日期；配 Amadeus Key 后全日期覆盖
- **斜纹/虚线日期是什么**：v0.8 的"临近日参考价"——该日源端无缓存价，显示最近有价日的参考（标注了距离天数）；实际价格点进购票页确认
- **琥珀色斜纹日期是什么**：v0.11 的"插值估算价"——两侧真实价按距离线性插值的估算（±10 元精度），展示用；实际价格点进购票页确认
- **为什么有的日期连插值都没有**：两侧 14 天内找不到真实价（源端库存未加载），只能给灰色临近日参考

## 七、备份与迁移

- **换电脑/迁移**：整个文件夹拷走即可；最关键的是 `config.json`（线路+价位+推送 key），`data/` 可以不带（会自动重建）
- **多设备**：一台主力机跑监控即可；其他设备要么收推送，要么局域网看面板，不需要重复部署

### Docker 部署（v0.17 新增，推荐长期运行）

单容器同时跑 Web UI 与定时采集，数据/配置落在宿主机目录，容器随便重建升级：

```bash
# 1. 准备配置(首次)
cp config.example.json config.json
# 2. 构建并启动(后台常驻, 开机自启随 Docker)
docker compose up -d --build
# 3. 看日志 / 停止
docker compose logs -f
docker compose down
```

浏览器访问 http://localhost:8765 。密钥(Bark/ServerChan/Amadeus)直接在 Web UI 里配，写入挂载的 config.json；data/ 目录含快照、缓存与日志，重建容器不丢数据。部署健康检查：GET /api/health 一次返回快照新鲜度(age_min)、板库沉淀进度、推送就绪度，可直接接入任何外部 uptime 监控轮询。不想装 Docker 时，原 start_windows.bat / start_mac.sh 方式完全不受影响。

## 八、使用边界

本组件只做**个人低频比价提醒**：不破解接口签名、不绕过验证码、分钟级查询频率、只读公开数据、购票永远跳转官方页面人工完成。请勿改造为商业爬虫或高频抓取。

## 九、Agent 接入（MCP Server）

v0.11+ 自带一个**零依赖的 MCP Server**，让 Codex / Claude 等 AI 客户端直接用自然语言操控监控：

```powershell
python mcp_server.py
```

在 Codex 的 config.toml（或 Claude Desktop 的 claude_desktop_config.json）里挂载：

```json
{
  "mcpServers": {
    "fare-alert": {
      "command": "python",
      "args": ["C:/path/to/fare-alert/mcp_server.py"]
    }
  }
}
```

挂载后直接对 Agent 说一句话即可建监控，例如 **"五一杭州飞成都800以内帮我盯着"** —— Agent 会解析城市对/阈值/节假日并写入线路；下一轮询周期生效，低于阈值走 Bark/微信推送。

| 工具 | 作用 |
|---|---|
| fare_search | 查未来 N 天含税最低机票价 TOP10（附购票直达链接） |
| train_search | 查某日 12306 车次全席位票价（含卧铺/二等座） |
| watch_add | 一句话新增监控线路（写前自动备份 config.json） |
| watch_del | 按 id 删除线路 |
| snapshot_get | 读最新快照：每线路最低**真实**含税价（估算价不计入） |

协议自检：python tools/mcp_selftest.py（握手/五工具/写入清理往返/异常帧防崩溃，8 项）。

## 十、设计系统（v0.28 起）

界面所有视觉决策收敛到 webui/static/style.css 顶部 `:root` 的**设计令牌**，新增样式一律从令牌表取值：

| 类别 | 令牌 | 说明 |
|---|---|---|
| 间距 | `--sp-xs/1/2/sm/md/3/lg/xl/4/2xl/3xl` | 2~40px 阶梯；奇数值(5/7/9px)属遗留，逐步迁入偶数阶 |
| 字号 | `--fs-2xs ~ --fs-7xl` | 8.5~30px 十三档；历史半像素值(12.5px 等)已并入整数档 |
| 圆角 | `--r-1/xs/sm/md/lg/pill/flag` | 2~20px + 胶囊 + 旗形特殊角 |
| 色板-中性 | `--bg/card/card2/line/line-soft/line-strong/text/text-2/muted/faint` | 背景到文字十阶 |
| 色板-品牌 | `--accent/accent-2/accent-ink/accent-soft/on-accent` | `--on-accent` 专用于深色底上的文字 |
| 色板-状态 | `--green(-d/-bg)` `--amber(-d/-bg)` `--red(-d/-bg/-line)` | 各状态族的文字/深色/浅底三件套 |
| 色板-功能 | `--gold(-ink/-bg/-soft)` `--info/-info-2/--sky/--violet` | 席位、列车、图表专用色 |
| 动效 | `--dur-fast/base` `--ease-out` | 过渡时长与缓动曲线 |

**门禁**：`python tools/ui_check.py` 含 v0.28 断言——规则体**零硬编码色值、零 font-size 像素值**，违反即 FAIL。改 UI 前先看令牌表，表里没有的值先扩表再使用。

## 十一、v2 新版前端（web/ 工程，v0.29 起）

前后端分离第二阶段已落地：新界面在 **http://127.0.0.1:8765/v2**（经典版仍在根路径，两者可随时互切，顶栏有入口）。

### 使用者（什么都不用装）

- v2 页面由后端直接托管，**不需要安装 Node.js**——构建产物已提交进仓库。
- 已迁移页签（v0.29–v0.30）：仪表盘（KPI / 最优判定 / 日历⇄条形 / 起降时间线 / 趋势图十字线 / Top5 / 火车对比 / 目的地情报 / 浮动提醒）、爬虫监控（源健康 12 轮成功率 + 每步耗时/缓存/错误）、周报（走势 sparkline + 一键推送）、数据源（开关 + Amadeus 密钥 + 时刻库沉淀进度 + 覆盖体检）、推送（密钥表单 + 测试推送 + 浏览器通知权限 + 提醒历史）。线路管理/预算找目的地/日志暂用经典版。

### 开发者（改 v2 界面时）

```powershell
cd web
npm install     # 首次
npm run dev     # 开发服务器, 改动热更新, API 自动代理到 8765
npm run build   # 产出 web/dist/, 提交前必跑(后端托管的是 dist)
```

规范：`web/src/styles/tokens.css` 必须保持为经典版令牌的**超集**（只能加不能删）；`app.css` 零硬编码色值/字号（`tools/ui_check.py` 会拦）；数据口径以 `web/src/lib/data.js` 为准，与经典版 `app.js` 语义对齐。改完跑 `npm run build` + 重启面板 + `python tools/ui_check.py` 三件套。
