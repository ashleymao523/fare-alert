# 迭代路线图（M0-M5）

> 方法论：每模块走 **规范 → 实现 → 验收 → 复盘** 四步，一个模块一次提交；
> 验收标准见 docs/验收规范.md，DoD 未达成不进下一模块。

## 一、里程碑总表

| 里程碑 | 模块 | 关键功能点 | 验收门 | 状态 |
|---|---|---|---|---|
| M0 | 工程地基 | AGENTS.md、验收规范、acceptance.py、单测；剩余：README 重写(GIF+3命令启动)、GitHub Actions CI(G0+G1+G6) | G0-G6 | ✅ 本提交 / 🔄 README+CI 待做 |
| M1 | Agent 接入层 | MCP Server：fare_search / train_search / watch_add / watch_del / snapshot_get 五个工具；自然语言意图→线路配置生成 | G0 G1 G2 G4 | ✅ 完成（独立 E2E 验收 + blocker 修复：batch 帧崩溃/通知回复/方向词反转/off-by-one） |
| M2 | 源健康自愈 | 爬虫健康分、连续失败降级徽标、诊断探针自动化、修复建议报告 | G1 G4 | ✅ 完成（健康分滑动窗口/双阈值降级/诊断分类报告/UI 徽标/MCP 透出降级源，单测 7/7；探针自动化与自动修复留 M2.1） |
| M3 | 智能查询 | 反向目的地搜索(预算优先)、多城市并行、节假日日历联动 | G2 G5 | ✅ 首版完成（预算过滤含税口径/请求硬预算(默认8·上限15·失败不重试)/6h结果缓存且命中不耗预算/UI页签+直达链接/MCP reverse_search 工具,单测 5/5+真实E2E 通过;多城市并行、节假日日历联动留 M3.1） |
| M4 | 洞察周报 | 价格历史归档、周度统计摘要、自然语言周报推送 | G4 | ✅ 首版完成(每日指标归档180天/周最低·均价·环比·最佳出发日,数字可从history重算,单测4/4+G4一致性门/周报页+走势图/推送默认关闭·7天冷却·UI开关;子agent复核PASS,3个P2已修:0价环比除零防护/归档路径健壮化/设置页周报推送开关;Agent LLM润色周报留 M4.1) |
| M5 | 插件生态 | 数据源适配器接口、回放夹具(fixture)、社区适配器模板 | G1 G5 | 未开始 |

## 二、各模块 DoD（完成定义）与测试实例

**M1 MCP Server**
- DoD：MCP 握手成功；五个工具入参/出参过 schema 校验；意图解析单测（"五一杭州飞成都800以内"→ 正确 route 配置）；E2E：任一 Agent 会话一句话建监控 → snapshot 出现该线路。
- 测试实例：①握手自测脚本 ②意图→config 10 组单测 ③watch_add 写入 config 后 acceptance 全绿。

**M2 源健康自愈**
- DoD：注入模拟故障（改坏端点）后 1 个轮询周期内检出并降级标记；UI 显示降级徽标；生成含候选原因的诊断报告；真实源恢复后自动解除。
- 测试实例：①故障注入单测 ②健康分计算单测 ③诊断报告结构断言。

**M3 反向搜索**
- DoD："¥X 以内从 A 出发"返回目的地列表（含税口径、带数据新鲜度标记）；单次查询总请求数 ≤ 限速预算。
- 测试实例：①预算过滤单测 ②请求预算计数单测 ③API 契约测试。

**M4 洞察周报**
- DoD：周报中每个数字可从 snapshot 重算一致；推送可关闭；语言为自然中文。
- 测试实例：①统计口径单测 ②周报数字与 snapshot 一致性(G4 扩展)。

**M5 插件生态**
- DoD：新数据源只需实现适配器接口 + 通过回放夹具，不改动核心代码。
- 测试实例：①示例适配器过夹具 ②夹具数据与真实响应结构 diff 为空。
- 状态：✅ 2026-09-10 交付（core/adapters.py 接口+注册表+一致性校验；
  qunar-calendar / qunar-intl / amadeus-intl 三个现有源已包装注册；
  tests/test_adapters.py 8 用例 + 3 回放夹具全绿；接入指南见 adapters.py 模块头）。

## 三、Agent 能力矩阵（谁执行什么意图）

| 用户意图 | 承载模块 | Agent 职责 | 人职责 |
|---|---|---|---|
| "帮我盯着某线路低于 X" | M1 MCP | 解析意图→调 watch_add→回报确认 | 确认配置、接收推送 |
| "接口怎么挂了" | M2 自愈 | 跑诊断探针→定位→给修复建议或自动改 | 审批高风险改动 |
| "X 预算能去哪" | M3 反向搜索 | 组合查询→汇总对比→给建议 | 定夺目的地 |
| "这周价格怎么样" | M4 周报 | 统计分析→写自然语言摘要 | 阅读决策 |
| "加个新数据源" | M5 插件 | 按模板写适配器→过夹具→发 PR | 审查合并 |
| "修 BUG / 加功能" | 本仓库 | 按 AGENTS.md 工作流改码→过验收门 | 验收成果、提需求 |

## 四、功能池（候选，按需领走）

door-to-door 总成本（机场大巴/时间价值 vs 高铁站）· 准点率数据 · 节假日抢票日历 · Telegram/邮件推送渠道 · PWA/iOS 快捷指令入口 · 匿名脱敏价格共享数据集 · 多用户部署 · 航变监控 · 酒店打包价。

## 五、研究归档区（探针结论，持续追加）

**v0.11 无价日期与时刻源研究（2026-09）**：去哪儿 H5 按日航班列表需 Bella 签名（红线，放弃）；携程 PC/H5 全 432 保护；飞常准 406/502；Aviasales 免Key日历已下线 404。结论：部分日期源端本身无价（郑州/曼谷），合规方案为两侧真实价插值（v0.11 已上线）；真实起降时刻唯一合规通道为 Amadeus（可选，涉隐私可不接）。

**M5 适配层落地面（2026-09-10）**：统一入参 FetchQuery（城市+区间+税费+密钥+缓存目录）；校验器 validate_deals 同时服务测试与运行时（日期格式/区间/正价/source 对齐/排序五断言）；ReplayAdapter 支持离线回放，新源开发不依赖外网。已知边界：train 适配器接口已定义（TrainSourceAdapter）但 12306 包装待 M5.1，编排层（crawl/travel）切换到适配器调用待 M5.2。

**v0.18 航班时刻零密钥源研究（2026-09-10）**：萧山机场官网「航班信息」页使用公开只读接口 apisys.h22.66571.com/api/leave_import/hbh/ 与 /arrive_import/hbh/（需 Referer: www.hzairport.com/flight/index.html，浏览器同源、无签名无验证码）；字段 hbh=航班号、main_hbh=共享号、jhsj=计划时间（出发板=起飞/到达板=落地）、preschtime=前站计划起飞、chinese_sfcs/mdcs=城市、chinese_hs=航司。方案：每日低频抓当日双板（6h 落盘缓存，日请求≤2），按「航班号+星期几」沉淀周班期参考时刻库（core/sched_board.py），到达板双时刻优先于出发板仅起飞；约一周自然覆盖 60 天日历内各航班的计划起降。时刻为航季计划参考（非实时验证），仅展示不打扰提醒逻辑；配置 Amadeus 后自动让位实时刻。否决项：重庆机场官网域名已停放不可用；AviationStack 免费套餐仅实时状态无未来时刻表。

**v0.18 复核修复（同日，双 agent 评审驱动）**：① 合并逻辑改「双时刻行整条替换、残缺行只填不删」，杜绝到达板缺 preschtime 时抹掉已存起飞时刻；② 每板每日≤2 次真实请求由计数文件（board_fetch_log）硬性落地，失败尝试同样计数（端点故障不会变成 45 分钟重试风暴），超限服务陈旧缓存，force 可越过；③ 库与缓存改原子写（tmp+os.replace，pid 后缀防互踩），读到坏库先备份 .corrupt-* 再重建，跨进程不再互毁；④ 推送渠道非 200 / ServerChan 错误体显式标 ERR（BODY code 与 HTTP 区分），周报全渠道失败写 6h 退避，部分成功消耗计时器并告警；⑤ 相关性门读 config 真实键 from_city/to_city（提成 touches_hangzhou 可测函数+回归用例，修复首版误读 from/to 导致恒 skip 的 P1），非杭州线路记 skip 省请求预算。新增 test_notify.py 与 guardrails/退避/门控用例，测试 35 项全绿。

**v0.19 跨日班期时刻补全（2026-09-10）**：实测 v0.18 覆盖极差的根因——班期库首周仅有当日 dow 数据（snapshot 中杭州-重庆 60 deals 仅 4 条 dep_time、0 条 arr_time），且官网接口无日期维度：页面 JS 只按 keywords=航班号搜索（jedate 只加载未使用），探测 ?date=2026-09-13 传参被忽略（返回仍是当日板）、路径段 /hbh/2026-09-13/ 按 keywords 处理（flag=-1 rows=0），**按日期回填方案否决，勿再消耗请求探测**。替代方案：低价日历已证明该航班号在该日期执飞，同号航班时刻按航季排班跨星期高度一致 → board_lookup_x 先精确 dow、未命中则跨 dow 借用（城市校验 + 双时刻条目优先），time_src=airport-board-x 打「跨日班期·参考」徽标；出发板只含起飞（HGH 出发板的 arr 永远不会有，落地属到达板特性），故仅有起飞时新增 arr_est 字段按大圆+约 40 分钟滑行/爬升（代码 0.7h）估算落地（跨零点回绕，~ 前缀 + 虚线样式 + 悬停说明），独立字段不写入 arr_time 防混源；中转行程同样走跨日借用且估算含 2.5h 中转，跨段徽标取弱标记（任一段借用即标跨日）。时刻纯展示不影响提醒/统计（红线#4 满足）。新增 board_lookup_x 7 项 + 估算/序列化 2 项单测，全套 42+8 全绿。

**v0.20 真 PWA + 时刻库透明度（2026-09-10）**：部署丰富度补齐——manifest 此前仅有 SVG 图标（iOS apple-touch-icon 不支持 SVG → iPhone 添加主屏后图标空白），无 Service Worker（无离线壳）。tools/gen_icons.py 用 PIL 以 4x 超采样绘制主题色渐变底+纸飞机，产出 icon-512/192/180/32.png 与 maskable-512/192.png（内容收进 78% 安全区），提交进仓库可复现；sw.js 以 fare-alert-v20 缓存桶实现 /static/* 缓存优先（URL 带 ?v= busted，升级即失效）+ /api GET 网络优先+缓存兜底（离线可读最近一屏数据，无缓存时返回 503 JSON）+ navigate 网络优先+缓存兜底（离线可冷启动 UI），非 GET 与跨域一律放行，activate 清旧桶，cachePut 失败静音防 unhandled rejection。新增 /api/sched-stats（core.sched_board.sched_stats 纯函数：非空条目才计数，避免进度条撒谎）+ 数据源页七天沉淀进度 widget（周一~周日高亮已沉淀天数+航班数），把「为什么这天没有精确时刻」从隐性知识变成显性 UI；周报推送无渠道报错时前端自动 gotoTab(push) 并高亮 Bark 输入框（推送引导闭环）。双 agent 验收（E2E 5/5 + 评审 0 P0/P1，P2 当轮修 4 项）。单测 44+8 全绿。已知遗留：mcp_selftest 对真实 config.json 做 add→del 恢复检查，与并发验收互跑会撞出假 FAIL（本轮两次实锤），下轮加文件锁或临时 config 副本隔离。

**v0.21 时刻覆盖仪表 + 自启 + selftest 隔离（2026-09-10）**：三项落地——① time_coverage(deals) 纯函数（core/flights.py）按 精确(amadeus/airport-board+时刻)/借用(airport-board-x)/估算(仅 arr_est)/缺失 四档统计每线路起降时刻，剔除 nearby-ref/interp 非真实价 deal，写入 snapshot 的 route.time_coverage，数据源页新增「本轮时刻覆盖体检」分组条形图（绿=精确/橙=借用/紫=估算/灰=缺失），时刻质量从黑盒变仪表；② mcp_server.py 支持 FAREALERT_HOME 环境变量覆盖落盘根目录，tools/mcp_selftest.py 改为 tempfile 临时副本运行（watch_add→watch_del 全程在副本内闭环），结束时字节级校验真实 config.json 未被触碰，修复 v0.20 两度实锤的并发验收假 FAIL（9 项检查）；③ tools/install_autostart.ps1 开机自启一键安装：默认走 HKCU Run 注册表键（免管理员、免计划任务策略），wrapper 先检测 8765 已监听则跳过（防重复拉起），-Uninstall 卸载、-Mode task 提供计划任务备选（受限主机需提权）。附带修复：周报页无推送渠道时「立即推送」按钮在 initWeekly 即禁用并悬停提示（原先点击后才报 400 再跳转引导，体验滞后）。新增 tests/test_time_coverage.py 5 项单测，全套 49+8 全绿。

**v0.22 经停友好时刻借用 + 按字段来源 + 推送修复（2026-09-11）**：① board_lookup_x 城市匹配改四层分级——tier0 双城匹配 > tier1 仅出发侧匹配（=经停航班，如 GJ8069 杭州→克拉玛依经停郑州，此前郑州线查询被整体拒绝）> tier2 仅到达侧 > tier3 双侧不匹配；精确 dow 命中恒为 exact（班期+dow+航班号唯一），跨 dow 借用只接受 tier0/1；实测修复：跑一天后 dow4 沉淀 3238 班，杭州-郑州 GJ8069 恢复 06:35 时刻。② FlightDeal 拆出 dep_src/arr_src 独立字段（此前单一 time_src 无法表达「起飞精确+落地借用」混合态），富化各分支按段标注，time_coverage 优先读分字段、回退 time_src 兼容旧快照；前端徽章按段显示（起飞·班期精查 / 起飞·跨日参考 / Amadeus），「另一段待补」按段判定。③ 离线重放验证：快照 deal 清空时刻后重跑富化，缺起飞 29→21（重庆 11→9、郑州 11→6），到达维持全 est 属已知边界（目的地侧无机场板）。④ 外部时刻源勘探全部失败记档：携程 whaleguard 432（无头亦拦）、去哪儿 H5 flightlist 需 Bella 签名（红线放弃）、飞常准 418、航旅纵横 403、cqairport.com 域名停放。⑤ 周报推送问题定位：config 无 Bark/ServerChan key → has_channel 恒 False → 周报推送静默跳过（非代码 bug，属配置引导）；测试推送连点会在 alerts.json 写重复行 → _record_alert 同秒同内容去重 + test-push 路由标记。⑥ autostart wrapper 读 config.json webui.port（默认 8765），换端口自启不再写死。新增跨 dow 分层 2 项 + 按字段拆分 2 项单测。

**v0.25 同航线真实时长先验(2026-09-11)**: 落地时刻估算从大圆圈几何模型升级为数据驱动——萧山到达板每日 1600+ 行双时刻(preschtime 前站起飞 + jhsj 落地)沉淀 83 城真实飞行时长中位数(build_route_priors, 45min~17h 垃圾过滤, min_samples=3), 直飞 arr_est/时长文本改用同航线实测值(重庆 140min/成都 155min/郑州 100min/曼谷 240min, 几何模型普遍高估 10-30 分钟); 国际前站城市归一化(曼谷素万那普机场→曼谷) + prior_minutes_for 前缀合并取最繁忙机场; 经停保持几何+2.5h 中转(先验仅单段, 不能映射多段)。红线: prior 只写 arr_est/duration_text, 不入 arr_time 不触发提醒。勘探负结论: 郑州 DNS 失败/重庆 cqa.com.cn 域名出售停放/成都双流 403/天府 DNS 失败——大陆目的地机场板源关闭; 返程方向(落地杭州)时刻可由萧山到达板精确命中, 开往返线路即自然生效(当前 4 线路均 oneway)。新增 sched_board 6 + core 1 单测, 全套 43+8 全绿。

**v0.25.1 评审加固(2026-09-11)**: 双 agent 验收(E2E 5/5 + 代码评审 PASS-with-notes)后集中修复——build_route_priors 加「全经停城市守卫」: 先验中位数低于大圆圈 900km/h+30min 物理下限的城市整城剔除(拉萨/敦煌这类无直飞城市的末段时长伪装成全程, 拉萨 145min vs 真实 ~280min), 43 城先验全部通过且最小余量 11min(丽江); codeshare 去重 key 改用解析后分钟数("8:00"/"08:00" 也去重); 周报引导步骤编号改为动态序号(修复 ①→③ 跳号), 购票链接补 rel="noopener", 落地时刻 tooltip 点名「萧山机场到达板」; check_priors.py 显示真实样本数 n, 周报无历史跳过补 debug 日志。新增 4 单测(全经停守卫/全簇不足剔除/三簇竞争/17h 上界), 全套 43+8 全绿。

**v0.26 无号日历价参考班次 + Docker 部署修复(2026-09-11)**: 时刻覆盖攻坚——Amadeus 日历价(曼谷 48/50 deal)只有价格+日期无航班号, 新增 city_dep_times(city, dow, limit) 按城市前缀+星期从萧山板库取参考班次(codeshare 同时刻去重, exact/cross-dow 双标记), 无号 deal 挂 alt_times 展示「当日参考班次」chips(exact 蓝/参考灰), 红线: 只做展示不写 dep_time 不触发提醒; 国内缺失主因是班期库仅沉淀 2 天(周四/五), 非每日航班(如 HO2043)随每日采集 ~7 天自然长满(板 API 无日期参数不可回溯)。Docker 修复: start.sh 补 --loop(旧写法无参跑一轮即退出, 调度假死) + mkdir + trap, 端口统一 8765(与 guide/config 一致), 移除 docker-entrypoint.sh 双入口混淆。推送健康徽标: /api/snapshot 返回 push_pending(weekly 开启但无任何密钥), 顶栏「提醒未配置」一键直达推送页。新增 4 单测, 全套 43+9 全绿。

**v0.26.1 参考班次补挂修复(2026-09-11)**: v0.26 挂载点选错位置——曼谷 48 个无价日期实际由 nearby-ref(邻近参考价)在 _enrich_flight_times 之后补齐, enrich 循环内的挂载点永远看不到它们, 快照 alt_times 恒为空。新增幂等 _attach_alt_times(deals, to_city, db): 在 _fill_reference_deals 之后对无号/无时刻/无 alt_times 的 deal 按日期缓存补挂; 只挂去程(板库是萧山出发板, 返程方向无数据, 注释说明)。离线端到端验证: 曼谷 50 deal 修复后 48 个挂上(周二日期靠跨班期借用全 exact=False, 合理)。Zeno P2/P3 同轮修复: 测试归位 TestRoutePriors + applyPushPending 抽公共函数(/api/run 后徽标实时刷新) + start.sh 双后台 trap wait 兑现优雅关停。test_core 9→11, 全套 43+11 全绿。

**v0.27 覆盖率合并口径 + 部署可观测(2026-09-11)**: KPI「起飞时刻覆盖」大数字从 dep-only 口径改为 精确+参考 合并口径(曼谷 4%→100% 的观感落差消除), 副文案拆「精确 x 天 · 参考 y 天」; 新增 /api/health 一站式健康端点(snapshot age_min + 板库航班数/星期覆盖 + push_pending, 可被任何外部 uptime 监控轮询); 顶栏「更新于」升级为新鲜度指示(<60min 绿 / <100min 黄 / 更旧红, 阈值避开 45+10min 调度抖动窗口)。部署不再是一个黑盒: 新鲜度、板库沉淀进度、推送就绪度一次可见。
