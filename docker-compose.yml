services:
  fare-alert:
    build: .
    container_name: fare-alert
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data        # snapshot/cache/sched-db persist on host
      - ./config.json:/app/config.json   # edit config on host, restart to apply
    environment:
      - FAREALERT_HOST=0.0.0.0
      - FAREALERT_PORT=8765
