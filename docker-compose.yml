services:
  fare-alert:
    build: .
    container_name: fare-alert
    ports:
      - "8765:8765"
    volumes:
      # 数据缓存/快照/日志落宿主机, 容器随便重建
      - ./data:/app/data
      # 密钥配置不入镜像, 挂载本地文件(首次从 config.example.json 复制)
      - ./config.json:/app/config.json
    restart: unless-stopped
